# 基础设施适配实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 Campus Agent MVP 增加可测试的 Gorm/MySQL 仓储适配和 RabbitMQ producer 适配。

**架构：** 领域层继续暴露接口，`internal/repository/mysql` 负责 Gorm 实现，`internal/mq/producer` 增加 RabbitMQ 实现并保留内存实现用于测试和本地装配。单元测试使用 SQLite 内存数据库验证 Gorm 行为，不依赖本机 MySQL。

**技术栈：** Gorm、MySQL driver、SQLite test driver、RabbitMQ `amqp091-go`、Go testing。

---

### 任务 1：Gorm TaskRepository

**文件：**
- 修改：`internal/repository/mysql/task_repository.go`
- 创建：`internal/repository/mysql/task_repository_test.go`

- [ ] **步骤 1：编写失败测试**

覆盖保存任务、按 ID 查询任务、更新状态和结果。

- [ ] **步骤 2：实现 Gorm 模型与仓储方法**

使用 `*gorm.DB` 注入，不在 repository 内部创建连接。

- [ ] **步骤 3：运行测试**

运行：`go test ./internal/repository/mysql`

### 任务 2：Gorm ChatRepository

**文件：**
- 修改：`internal/repository/mysql/chat_repository.go`
- 创建：`internal/repository/mysql/chat_repository_test.go`

- [ ] **步骤 1：编写失败测试**

覆盖保存消息、按用户倒序读取并限制数量。

- [ ] **步骤 2：实现 Gorm 模型与仓储方法**

`ListByUser` 返回按 `created_at desc, id desc` 排序的消息。

- [ ] **步骤 3：运行测试**

运行：`go test ./internal/repository/mysql`

### 任务 3：RabbitMQ Producer

**文件：**
- 修改：`internal/mq/producer/producer.go`
- 创建：`internal/mq/producer/rabbitmq_test.go`

- [ ] **步骤 1：编写失败测试**

覆盖任务消息 JSON 编码和 routing key 选择。

- [ ] **步骤 2：实现 RabbitMQ producer**

通过小接口封装 `PublishWithContext`，让测试不需要真实 RabbitMQ。

- [ ] **步骤 3：运行测试**

运行：`go test ./internal/mq/producer`

### 任务 4：装配与验证

**文件：**
- 修改：`README.md`
- 修改：`deployments/docker-compose.yml`

- [ ] **步骤 1：更新 README**

说明当前仓储和 MQ 适配层状态，以及本地中间件启动方式。

- [ ] **步骤 2：全量验证**

运行：`go test ./...`
